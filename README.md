# MuniServe

Multi-LGU electronic Business Permit and Licensing System (eBPLS) for the Philippines, piloting with San Miguel, Bulacan.

See [`CLAUDE.md`](./CLAUDE.md) for the full build specification — non-negotiable rules, schema, RLS, workflow state machine, and build order. Read that before touching anything in here.

## Status

Live in production: Supabase, GitHub, Vercel, fee rules, legacy business data, and staff auth are all wired up and verified working end-to-end.

- [x] Next.js (App Router, TypeScript, Tailwind) scaffold
- [x] Database schema drafted — [`supabase/migrations/0001_initial_schema.sql`](./supabase/migrations/0001_initial_schema.sql)
- [x] RLS policies drafted — [`supabase/migrations/0002_rls_policies.sql`](./supabase/migrations/0002_rls_policies.sql)
- [x] `reference/` populated — fee computation source, HTML prototypes, legacy business data, and the unreconciled fee guide (see `reference/README.md` for what's in there and why)
- [x] Supabase project created and migrations run (schema + RLS both verified live)
- [x] GitHub repo created and pushed — [infomuniserve-creator/muniserve-](https://github.com/infomuniserve-creator/muniserve-)
- [x] Vercel project connected, deployed, env vars set
- [x] Seed script written and run against the live DB — `supabase/seed/seed_san_miguel_fee_rules.sql` (+ migration `0003_fee_rule_bracket_rate_basis.sql`). 112 fee_rules (88 active, 24 inactive pending BPLO confirmation), 139 fee_rule_brackets. See CLAUDE.md section 7a for discrepancies resolved while writing it.
- [x] Bulk-import legacy business roster into `businesses` — 1,177 imported (`bt_source='actual'`), 168 held out for manual review (`needs_review`, matches `BPLO_LBT_NeedsReview.csv`), 5 held out for missing license numbers (`no_match`). See `supabase/seed/legacy_business_import.sql`.
- [x] Staff auth + dashboards — Google OAuth via Supabase, verified end-to-end in production (real sign-in → `staff_users` lookup → role-routed → RLS-scoped queries). BPLO dashboard confirmed working live. Department and mayor dashboards share the same code path but haven't been tested with real accounts yet — provision a `staff_users` row for those roles when ready to check them too.
- [x] Applicant OTP flow + application form + legacy-claim flow — verified end-to-end live: real SMS via Semaphore, real OTP, real legacy business claimed (license 7094956), real application submitted as reference `MS-2026-00001`.
- [x] Review workflow state machine wired end-to-end (`review_rounds`/`department_reviews`, parallel department fan-out, BPLO proxy decisions, resubmission scoped to flagged departments only) — see `src/lib/review-workflow.ts`.
- [x] Applicant form rebuilt against San Miguel's real, currently-live intake form (not the earlier mockup) — see CLAUDE.md section 7d and `reference/official-application-form/`. Adds ~40 fields across registration, structured address, business operation, and 9 nature-of-business-conditional groups, plus the full conditional show/hide logic, a manual BPLO LBT-category override (stopgap until the fee engine exists), and an expanded staff-facing business profile view. Document upload/signature-capture code is untested via browser (file-picker automation limitation) but follows the same proven auth pattern as the rest of the applicant routes. Migration `0009_official_application_form_fields.sql` is live in production.
- [x] Staff dashboard redesign + Business Registry — see CLAUDE.md section 7e. Card-based, soft-rounded design system (Quicksand/Nunito, brand navy→teal, light+dark, real dark-mode toggle) applied across all four role dashboards, shared `ui.tsx` component library. Business Registry Directory (`/dashboard/businesses`, all roles, read-only) lists every business on file with a derived status (active/needs renewal/legacy/inactive/in progress) and search/filter; BPLO can start a renewal or reactivation for a walk-in owner directly from a row. Migration `0010_bplo_walkin_application_insert_policy.sql` is live in production.
- [x] Permit History — see CLAUDE.md section 7f. Second view alongside the Directory: a dense, sortable/filterable table of San Miguel's **13,548 real historical permit records (2020–2026)**, imported from the project owner's reference dashboard (data only — no credentials committed; see 7f for the exposed-API-key flag). New `permit_history` table, migration `0011_permit_history.sql`, live in production and fully imported. Caught and fixed a real bug along the way: Supabase's silent 1,000-row response cap was already under-populating the Directory's own businesses query (San Miguel has 1,177) — both now paginate properly via `src/lib/db-pagination.ts`. Kept from going stale: every Mayor sign-and-release now appends a `permit_history` row too (migration `0012_permit_history_insert_policy.sql`, live in production) — the log grows with real MuniServe activity instead of freezing at the 2020–2026 import.
- [x] Fee computation engine — see CLAUDE.md section 7g. Computes the LBT schedule line, essential-commodity discount, Mayor's Permit line (business-type-specific or standard fallback), and CEDULA for every application, wired live into BPLO's assessment step (a real preview, computed fresh each render, with per-line override inputs) and Treasury's payment step (real assessed breakdown instead of blind manual entry). Verified against real production fee rules with six synthetic scenarios before shipping — caught and fixed a real bug (per_unit rules need their flat base amount added, not just the per-unit rate). Real data gaps (liquor sub-type, carnival operating days, missing LBT category) are flagged for manual assessment rather than guessed.
- [x] Applicant form: identity fields moved onto the main form + progressive disclosure — see CLAUDE.md section 7h. First/Last Name, Email, and Owner's Gender are now editable inline on every submission (pre-filled from the owners record, phone shown read-only since it's the OTP-verified session identity), matching the real production form instead of a separate one-time "identity" screen. Documents/Signature/Declaration/Submit now stay hidden until the rest of the required fields are filled in, also matching the real form's behavior.
- [x] Two missing pipeline stages added: printing and release — see CLAUDE.md section 7i. The workflow state machine now has `pending_printing` and `pending_release` between payment and "released" (migration `0013_printing_release_pipeline_stages.sql`), each with its own BPLO confirmation step, and every place the pipeline is displayed (staff `WorkflowStepper`, the applicant status tracker, BPLO's dashboard) was updated to the corrected 8-stage order. Staff dashboard container widened from 768px to 1280px along the way.
- [x] Notifications — see CLAUDE.md section 7j. SMS (Semaphore) to the applicant at every point the ball moves into/out of their hands (returned to applicant, department reject/request-info, assessment ready, payment received, released), plus email (Resend, new `src/lib/resend.ts`) to BPLO on a department issue and to departments on the 24-hour reminder cron already specified in section 6 (`vercel.json` + `/api/cron/department-reminders`, needs `CRON_SECRET` set in Vercel's own env vars too). All logged to `notifications_log` (existed unused since migration 0001). Escalation tier deliberately not implemented — timing still unconfirmed (section 10). `RESEND_FROM_EMAIL` needs a real address on a domain verified in Resend before staff email actually sends.
- [x] Permit PDF + QR generation — see CLAUDE.md section 7k. `src/lib/permit-pdf.ts` (pdf-lib + qrcode, no native deps) generates a from-scratch permit certificate + QR code at signing (`mayor/actions.ts`'s `signPermit`), uploaded to a new public `permit-pdfs` bucket (migration `0014_permit_pdfs_bucket.sql`, live in production). The QR code links to a new public, unauthenticated `/verify/[reference]` page — deliberately different from the owner-gated `/status/[reference]`, since anyone scanning a posted permit needs to verify it without signing in. `/status/[reference]` now shows a "Download permit (PDF)" link once released. Caught and fixed a real bug before shipping (verified by rendering an actual test PDF, not just reading the code): a date-only field rendered one calendar day early due to a timezone rollover. Needs `NEXT_PUBLIC_APP_URL` set in Vercel's own env vars (placeholder already in `.env.local`).
- [x] Custom domain — see CLAUDE.md section 7l. Live at `portal.muniserve.ph` (a new subdomain on the project owner's existing `muniserve.ph`, alongside their unrelated GoHighLevel setup on `links.muniserve.ph` — different hostnames, no conflict). Caught and fixed a real gotcha: Google sign-in kept landing back on `muniserve.vercel.app` regardless of which domain started the flow — not an app bug, Supabase Auth's own Redirect URLs allowlist only had the old domain on it.
- [x] Staff account management UI — see CLAUDE.md section 7m. BPLO can now add/deactivate `staff_users` accounts at `/dashboard/staff` themselves — previously an explicit service-role/admin-only task (migration 0002's own comment said so), meaning onboarding a new department reviewer required direct database access. New accounts are added by email only and self-link on that person's first real Google sign-in (migration `0015_staff_management.sql`, live in production). Guards against deactivating the last active BPLO account at an LGU. Adding a staff member now also emails them a sign-in link automatically (needs `RESEND_FROM_EMAIL` set to a real verified address to actually send — same pending item as the notifications work above).
- [x] Multi-LGU display fixes — see CLAUDE.md section 7n. The LGU name/letterhead (flagged hardcoded twice already) now comes from data (`lgus.display_name`/`bplo_office_name`, migration `0017_lgu_display_fields.sql`, live in production) across every dashboard page, the permit PDF, `/verify/[reference]`, the applicant form, and the login/landing pages — each resolved via whichever LGU is actually correct for that page (a staff session's own LGU, a permit's own application, or the pre-auth pilot placeholder). Explicitly **not** solved here: which LGU an anonymous visitor belongs to (still no URL-based LGU routing) and `san-miguel-form-options.ts`'s barangay/nature-of-business lists (genuinely per-LGU reference data, not text). Caught a real build-breaking gotcha along the way: `apply`/`login`/the landing page used to be static and had to be marked `force-dynamic` once they started reading from the DB, or a build-time DB hiccup could fail the whole deployment.
- [x] Multi-tenant platform-admin: subdomain routing + client onboarding — see CLAUDE.md section 7o. Real per-request LGU routing now exists (`lgus.subdomain`, migration `0018_multi_tenant_platform_admin.sql`, live in production) — `resolveLguId`/`resolveLguDisplay` read the request's own Host header (no proxy/middleware file needed) and replace 7n's pilot-only placeholders in `apply`/`login`/the landing page. Fixed a real correctness gap 7n's display-only pass left behind: `submit-application`/`lookup-license` still filed every applicant's real data under San Miguel regardless of branding — now resolved per-host too. New `platform_admins` role (separate table, claim-by-email like staff) gets a self-service `/admin` UI to onboard a new LGU client end-to-end (LGU row, departments, bootstrap BPLO account, welcome email to the shared login) — bootstrap admin account is `info.muniserve@gmail.com`. Deliberately not automated: `fee_rules` seeding (ordinance-dependent, section 7b) and each new client's own public subdomain (needs a manual Vercel+DNS step, same as `portal.muniserve.ph`, section 7l).
- [x] Platform-admin "View as": full troubleshooting access to any client's dashboard — see CLAUDE.md section 7o's same-day follow-up. `/admin` now has a "View as" control per client (BPLO, Treasury, Mayor, or a specific department) that upserts one reusable `staff_users` proxy row (migration `0019_platform_admin_view_as.sql`, live in production, `staff_users.is_admin_proxy`) and drops the platform admin straight into that client's real dashboard — full read/write, identical to genuine staff, with zero new RLS policies needed anywhere (the proxy row's own real `auth_user_id` + a real `lgu_id` already satisfy every existing staff-scoped policy). No staff account is ever added to a client's own roster to make this work, now or in the future. A persistent banner on every `/dashboard` page makes it unmistakable when the admin is viewing as a client rather than their own account, and every action taken while proxied is attributed to "(Platform Admin)" — never silently as that client's own staff.
- [x] Pause and delete a client from `/admin` — see CLAUDE.md section 7o's same-day follow-up. **Pause** (`lgus.is_paused`, migration `0020_lgu_pause.sql`, live in production) blocks that client's own staff from signing in — `dashboard/layout.tsx` shows a full-page "Account Paused" notice (contact `hello@muniserve.ph` / `0977-440-1374`) instead of any dashboard page — without touching any of their data or affecting the platform admin's own "View as" access. Also blocks the public `/apply` form for that client's subdomain, showing applicants a friendlier "temporarily unavailable, visit the office in person" notice with no mention of MuniServe or the pause itself — a deliberately different message than the staff-facing one, since an applicant has no part in the LGU's own billing relationship with MuniServe. `/status/[reference]` for already-submitted applications is unaffected. **Delete** only succeeds when a client has zero applications and zero businesses on file (real usage makes a raw delete fail on foreign-key constraints anyway, by design — a client with real data should be paused, not deleted) and requires typing the exact client name to confirm.
- [x] Applicant form picklists made multi-LGU — see CLAUDE.md section 7o's same-day follow-up. New `lgu_form_options` table (migration `0021_lgu_form_options.sql`, live in production, seeded with San Miguel's existing 49 barangays + 220 nature-of-business options via `supabase/seed/seed_lgu_form_options.mjs`) replaces `san-miguel-form-options.ts`'s hardcoded lists as the applicant form's runtime source. Barangay has no cross-LGU fallback (degrades to free text until a client sets their own — a wrong municipality's barangay names would be actively incorrect, not just generic); nature-of-business falls back to San Miguel's list (a reasonable generic default that also keeps `application-form-logic.ts`'s conditional show/hide rules working, since those are hardcoded against these exact strings). `/admin`'s create-client form gained an optional comma-separated Barangays field. Rebuilding each client's own conditional form logic/field set (vs. just the picklist values) is explicitly out of scope — flagged, not silently assumed.
- [x] Surfacing each client's public application form link — see CLAUDE.md section 7o's same-day follow-up. `https://<subdomain>.muniserve.ph/apply` is now shown in three places: the welcome email sent at client creation, a clickable link on `/admin`'s client list, and a persistent card on `/dashboard/staff` (BPLO-only) — all three with the same caveat that it only resolves once that subdomain's DNS/Vercel setup is done.
- [x] Iframe embed code for the applicant form — see CLAUDE.md section 7o's same-day follow-up. `next.config.ts` now sets a real framing policy for the first time (`Content-Security-Policy: frame-ancestors 'self'` everywhere, overridden to `frame-ancestors *` on `/apply` only) — previously every page including `/dashboard`/`/admin` had zero framing restriction at all, a real clickjacking gap fixed alongside this feature, not just before it. `ApplyPageClient` auto-resizes via `ResizeObserver` + `postMessage` so an embedded iframe never shows an awkward fixed-height scrollbar. The embed snippet (`src/lib/embed.ts`, shown via a new shared `EmbedCodeBox` copy-button component) appears next to the plain apply link on both `/dashboard/settings` and `/admin`.
- [x] New `/dashboard/settings` page (BPLO-only) — see CLAUDE.md section 7o's same-day follow-up. Split out of `/dashboard/staff`, which was starting to carry two unrelated concerns (staff accounts vs. LGU-level configuration). Owns the "Your public application form" link/embed-code card; reached via a new gear icon in the dashboard top bar (`settingsHref`, gated the same way as the existing Staff tab). Built as a real page since more LGU-level settings are planned to land here. (Staff accounts moved into this same page as of section 7t, below — `/dashboard/staff` no longer exists as its own tab.)
- [x] Business Tax & Mayor's Permit Fee Setup: self-service CSV import — see CLAUDE.md section 7s. BPLO can now set or update Local Business Tax and Mayor's Permit Fee rates themselves from `/dashboard/settings` — download the current rates as a CSV, edit in Excel/Sheets, upload back with a plain-language preview before anything goes live — instead of needing a developer to hand-write a seed script or run raw SQL for every LGU onboarded. Two templates (`src/lib/fee-rule-import.ts`) cover both real LGU shapes seen so far (San Miguel's graduated brackets + named catalog, San Ildefonso's business-size tier matrix, from a real 2006 San Ildefonso Revenue Code). Migration `0028_bplo_lbt_mayors_permit_rls.sql`, live in production, is what makes this safe to open up to BPLO's own session.
- [x] Settings reorganized: staff management moved in, sections reordered — see CLAUDE.md section 7t. The standalone "Staff" top-nav tab is gone; adding/deactivating staff accounts is now Settings' first section, "Add/Remove Staff". "Your public application form" moved from first to last on the same page.
- [x] Fixed a schema-wide RLS bug: `current_staff()` picking the wrong staff row — see CLAUDE.md section 7u. Treasury's "Record payment" was throwing a server error every time for the project owner's own platform-admin test account (a bare RLS rejection, not a code bug in `recordPayment()`) — root cause was a SQL function nearly every RLS policy in the schema depends on returning a nondeterministic row for any account with more than one `staff_users` row. Migration `0029`, live in production, verified against the exact failing session.
- [x] BPLO can record a payment on Treasury's behalf — see CLAUDE.md section 7v. Closes a real gap: a walk-in applicant who already paid at the Treasury counter and brought their OR receipt straight to BPLO had no way to move their application forward before this. Same shape as the existing "BPLO can act on a department's behalf" rule, applied to Treasury — BPLO can confirm payment and record an OR number, never adjust what's owed. Migration `0030`, live in production. The queue itself (`AwaitingPaymentSection`) is now shared between both dashboards instead of duplicated.
- [x] Staff SMS + email notifications on every new queue item — see CLAUDE.md section 7w. Staff previously only heard about a new application, a department review, an assessment, or a document upload by checking their own dashboard cold. Added `staff_users.phone` (migration `0031`, live in production) plus an "Add/Remove Staff" phone field and per-row edit, and wired SMS+email into every real trigger point: new submission → BPLO, department round opens → that department, all departments clear → BPLO, assessment finalized → Treasury, payment recorded → BPLO, signed → BPLO (only if the Mayor signed it themselves), document uploaded → whoever can currently act. Same-day follow-up: no notification to the Mayor on "printed" (they never open the app for this at this pilot LGU — see the next line), and the applicant now hears "ready for pickup" the moment it's signed, not just once released.
- [x] BPLO can sign a permit on the Mayor's behalf — see CLAUDE.md section 7w's same-day follow-up. Matches the real physical process: BPLO carries the printed permit to the Mayor's office, gets it signed on paper, carries it back, and records it themselves — migration `0032`, live in production, additive alongside the existing mayor-only policies (a real Mayor login still works unchanged). New BPLO dashboard queue, "At the Mayor's Office."
- [x] Print-ready certificate at "For Printing" — see CLAUDE.md section 7x. Opening a "Ready to print" card now offers a real, already-filled PDF (permit number, business details, receipt/amount paid) instead of just a confirm button — built directly against a real photographed San Miguel Business Permit, not a guess. Deliberately separate from the existing post-signing QR'd PDF (no signature yet at this stage). New `lgus.mayor_name` (migration `0033`, live in production, editable in Settings) for the signature block.
- [x] Self-service permit print template upload — see CLAUDE.md section 7y. BPLO can now upload their own fillable-PDF certificate design in Settings — the system reads back the file's own real field names, BPLO maps each one to the right data point (permit number, business name, amount paid, etc.), and every application reaching "For Printing" uses it from then on, with the LGU's exact custom design (colors, seal, everything) preserved untouched. Falls back to the generated certificate if no template is set or filling one ever fails. New private `permit-print-templates` storage bucket + `lgus.print_template_path`/`print_template_field_mapping` (migration `0034`, live in production).
- [x] Reusable prompt for converting an LGU's Revenue Code into the CSV import — see CLAUDE.md section 7z. A documented, repeatable process plus a copy-pasteable prompt (embeds the full column schema, so it works standalone with no repo context) for onboarding future LGUs' fee rates. First real use against a fresh San Miguel ordinance scan correctly flagged real ambiguities and caught a real bug in `parseLbtCsv` — see 7s's same-day follow-up.
- [x] Engineering-assessed Building Permit Fee — see CLAUDE.md section 7aa. A new Settings toggle lets BPLO turn on a field for Engineering to enter their own computed Building Permit Fee (the real, variable, PD 1096 fee — researched and confirmed before building, distinct from a flat fee) directly during department review; Approve is blocked until an amount is entered, and it's automatically included in the assessment total once approved. Migration `0035`, live in production.
- [x] Click feedback on every button and nav tab — see CLAUDE.md section 7bb. Every button that triggers a server action (Approve, Settings toggles, Mark as printed/released, etc.) previously gave zero visual feedback while working. `PrimaryButton`/`OutlineButton`/`MiniButton`/`GhostButton` now show a spinner and go inert while submitting (`useFormStatus`, no new dependency) — fixed everywhere at once since every dashboard page already shares these four components. Nav tabs get a small pending hint too (`useLinkStatus`), on top of the existing route-level loading skeleton. Same-day follow-up: the nav bar itself (and its pending dot) used to live inside each page, so `loading.tsx` unmounted it along with everything else during a tab switch — moved into the persistent `dashboard/layout.tsx` (new `persistent-top-bar.tsx`) so it now survives every navigation instead of vanishing with the page it belonged to.
- [x] Downloadable submitted-form PDF — see CLAUDE.md section 7cc. Staff can download a non-editable PDF of exactly what an applicant (or BPLO, filing a walk-in) submitted for a given application. Found and fixed a real architectural gap first: submitted field values live on the mutable `businesses` row, not per-application, so an old application's download would've silently shown newer data after a later renewal — new `applications.form_snapshot` column (migration `0036`, live in production) captures the real values at submission time instead. Pre-migration applications fall back to reconstructing from current data with a visible disclaimer. Verified against real production data (San Miguel's first-ever test application), which caught two real rendering/data bugs before shipping.
- [x] Business Registry discoverability — see CLAUDE.md section 7dd. The click-to-expand business row already showed full details, just with a bare chevron as the only hint it was clickable. Added a visible "View details" label next to it.
- [x] Two real gaps in the legacy-claim/renewal identity flow — see CLAUDE.md section 7ee. Fixed a real bug where a legacy business could silently fail to ever link to its owner's account if their first attempt was "sign in with phone" instead of License Number lookup. Also closed a gap where a returning owner who lost access to their old phone number had no way back in at all — BPLO can now update the registered phone on an already-claimed business (new `owners` UPDATE RLS policy, migration `0037`, live in production), and the renewal lookup now tells a returning owner their business is already on file (with a masked phone hint) instead of a bare "no match found." Same-day follow-up: added a way for BPLO to attach a phone number to a still-legacy business with no application involved, and while verifying it against production data, found and fixed a real bug in the *already-shipped* walk-in flow — creating a brand-new owner was silently failing under RLS for the single most common way a legacy business gets claimed at the counter.
- [x] CEDULA: online vs. counter-paid, per LGU — see CLAUDE.md section 7ff. New Settings toggle lets BPLO choose whether CEDULA is folded into the online assessment total (issued as part of the application, no upload needed) or stays counter-paid at Treasury (today's default, applicant uploads proof) — `fee-engine.ts` already computed this from `fee_rules.delivery_mode`, but nothing ever let BPLO set it and the application form's upload requirement was hardcoded on regardless. New UPDATE-only RLS policy (migration `0038`, live in production); CEDULA's own amount stays fixed (national law, confirmed with the project owner, never LGU-editable). Same-day follow-up: a counter-paid CEDULA no longer shows up as a line on the assessment card at all (was previously still shown with its amount, excluded from the total but flagged live as confusing regardless) — verified against a real San Miguel application in assessment.
- [x] Renewal lookup: License Number → Permit Number, OTP to the phone on file — see CLAUDE.md section 7gg. A business registered fresh through MuniServe never gets a License Number at all — only a Permit Number (`applications.reference_number`, what's printed on the actual certificate). Renewal lookup now searches both, and an already-claimed match sends the OTP straight to the real phone already on file (never one the applicant types), replacing the earlier "already registered, visit BPLO" dead-end with something that resolves itself whenever they still have that phone. New `send-renewal-otp` route + a `businessId` branch on `verify-otp`; the real number is never sent to the client, only a masked hint. Verified end-to-end against San Miguel's own real test business, including one real SMS round trip.
- [x] Fixed the new renewal flow's "Documents section never appears" bug — see CLAUDE.md section 7gg's same-day follow-up. Reported live by the project owner testing their own renewal. Two real causes: the Business Operation section's existing cascading-disclosure chain is easy to miss on a pre-filled (sparse legacy) renewal since nothing signals which field is still blocking, and the new businessId-driven renewal path (the fix directly above this one) never populated the read-only "Mobile phone" field since the applicant never types it there. The blocked-section message now names the actual missing fields instead of a bare "fill in the required fields," and `verify-otp` now reports back the phone it verified against so that field populates on every path. Re-verified live against the same real business that surfaced the bug.
- [x] Assessment overrides: a live-updating total — see CLAUDE.md section 7hh. BPLO could type an override amount, but "Total due online" never reflected it until "Finalize assessment" was already submitted. Each override line now gets its own Save button (enabled once typed, so the total doesn't flicker mid-keystroke); clearing an override reverts the total immediately. Purely a client-side preview — `finalizeAssessment` itself is unchanged, since it already read straight from the form fields regardless of any "Save" state. New `AssessmentLineItems` client component replaces the old split between inline override inputs and a separate manual-entry section, combining both into one live total.
- [x] Order of Payment: itemized assessment slip (PDF + email + BPLO print/reprint) — see CLAUDE.md section 7ii. Built against a real physical San Miguel Order of Payment the project owner photographed. BPLO picks a required Mode of Payment when finalizing assessment; the generated PDF (Acct Code / Particulars / Amount, reading from `application_fee_lines` for reprint-accuracy) is emailed to the applicant (a deliberate one-off exception to the "applicants are SMS-only" rule — SMS can't carry an attachment), previewable/printable from the Awaiting Payment queue on both Treasury's and BPLO's dashboards, and reprintable later from the Business Registry's application history. New migration `0039` (acct_code, assessment_finalized_by, mode_of_payment, treasurer_name), live in production, no new RLS policies needed. Verified against a real assessed San Miguel application (including a real BPLO override and an excluded reference-only CEDULA line) and RLS-checked under a real non-BPLO staff session.
- [x] Per-LGU SMS Sender Name, centralized brand prefix — see CLAUDE.md section 7jj. Researched Semaphore's own API first (confirmed: one account/API key can hold several approved Sender Names, chosen per-message via a `sendername` parameter — no separate API/account needed per LGU). New `lgus.sender_name` (migration `0040`, live in production) plus a new Settings card lets an LGU that's purchased/registered one use it; every SMS that used to hardcode a brand prefix in 6 different files now goes through one centralized decision in `notifications.ts` — drops the prefix once a real Sender Name is set (it already shows as the "From"), otherwise falls back to `"BPLO: "` (changed from `"MuniServe: "` same day, a one-line change since it's centralized). Zero behavior change for San Miguel today beyond that prefix text (`sender_name` still null in production).
- [x] Fixed Permit History's Owner column being covered by the frozen columns — see CLAUDE.md section 7kk. The three frozen columns (Year/Permit No./Business Name) used a hardcoded pixel offset that never matched their real rendered widths, so the frozen Business Name column got shoved right past its natural position — even at rest, not just while scrolling — covering the start of the Owner column right after it. Fixed by giving the frozen columns real enforced widths and deriving the sticky offsets from them instead of guessing a second, independent number. Verified by measuring the real DOM (0px overlap, was previously covering the Owner column) against synthetic data matching the reported screenshot.
- [x] Renewal lookup field relabeled to "Permit No. or License No." — see CLAUDE.md section 7gg's same-day follow-up. The backend already searched both since 7gg's original build; the field was just labeled/worded as "Permit Number" only, giving no visible cue to legacy business owners (most of them) that their old License Number would also work. Copy-only fix — updated the field label, placeholder, and every related message (no-match error, confirm screen, phone-sign-in fallback). Verified against a real still-unclaimed License Number in production, confirming the relabeling didn't change the underlying lookup behavior.
- [x] Closing the "request more info" loop — see CLAUDE.md section 7ll. BPLO's own initial-review "Request more info" was a dead end (generic SMS, no note shown, no way to upload, no way back in but a phone call). New `info_requests` table (migration `0041`, live in production) is one shared mechanism behind all three reviewing surfaces — BPLO's initial review, any department, and a brand-new Treasury version (non-blocking, e.g. asking for a clearer copy of an attachment) — so the applicant's status page shows the actual note and an upload box, and uploading auto-routes the application straight back to whoever asked (a fresh department review round, BPLO's queue, or a Treasury notification) with no manual "resubmit" click needed. SMS now points to email + the status page instead of "contact the BPLO office." Verified with real RLS checks and a self-cleaning synthetic test run against production through all three scenarios.
- [x] Self-service barangay list in Settings — see CLAUDE.md section 7mm. Previously only settable once, by a platform admin, at client creation (`/admin`'s optional "Barangays" field) — no way to add or fix a client's list afterward short of raw SQL. New "Barangays" card in Settings (BPLO-only) lets them add one or many at once (comma-separated) and remove any, with a new additive RLS policy (migration `0042`, live in production) scoped to `option_type = 'barangay'` only. Verified against real production data: a real BPLO session can add/remove at their own LGU, a non-BPLO role is correctly denied, zero leftover test rows.
- [x] Barangay Clearance fee — see CLAUDE.md section 7nn. A real, confirmed gap: the applicant form has always asked "Yes" or "No, generate my Brgy. clearance," but nothing ever charged for the "generate mine" answer. New `fee_rules.fee_category = 'barangay_clearance'` (migration `0043`, live in production) — a Settings card sets one uniform rate plus optional per-barangay overrides (an override always wins, no separate mode flag needed). A missing rate is a soft warning in the live preview but a hard, toggle-independent block on actually finalizing the assessment. Verified against real production data: RLS checked, and the real fee-computation function run directly against San Miguel's real barangays confirmed all four cases (already has one, overridden barangay, uniform fallback, unconfigured warning).
- [x] Settings reordered + made fully collapsible — see CLAUDE.md section 7nn's same-day follow-up. "Barangays" now comes before "Barangay Clearance" (add the list first, then set rates against it). All eleven Settings sections — not just those two — now collapse via a new shared `CollapsibleSection` component, closed by default, reusing the same chevron mechanism the Business Registry's own expand rows already use. No new dependency, no client-side JS state.
- [x] Archiving "Returned to applicant" — see CLAUDE.md section 7oo. That queue had no date filter and nothing ever moved an application out of it, so it grew forever; "Rejected" turned out to be dead code, never actually set anywhere. New `archived` status (migration `0044`, live in production) reached only via a manual BPLO Archive/Reopen action — deliberately not an auto-expiry timer, which would mean guessing a "how many days" number with no real basis. "Returned to applicant" now collapses by default with an Archive button per row; a new "Archived" section holds archived ones with a Reopen button. Also fixed a proactively-caught gap in the Business Registry's own status logic, which didn't yet know `archived` counts as terminal. Verified against real production data (rolled-back RLS check against the real reported application, plus a synthetic self-cleaning round-trip test).
- [x] Phone re-verification on the applicant status page — see CLAUDE.md section 7oo's same-day follow-up. Testing the archive feature surfaced a real, already-documented gap: opening a "request more info" link from a different browser/device than the one an application was submitted from hit a dead end ("Can't verify this application here"), since the applicant session is a cookie tied to that one browser. New "verify with the phone on file" option sends an OTP (reusing the same OTP infrastructure as the renewal flow) and signs that browser in too on success. Verified functionally without spending a real SMS credit — synthetic application, direct OTP-code check, confirmed the session cookie actually took effect end-to-end.
- [x] Document uploads now bypass Vercel's function size limit entirely — see CLAUDE.md section 7pp. A "10MB max" upload error turned out to be hiding a real ~4.3MB platform ceiling that made the declared limit dead code — raising it further wouldn't have helped. Real fix: the browser now uploads straight to Supabase Storage via a signed URL, never through our own server, so scanned documents up to 25MB work (bucket-enforced, not just app-checked). Verified against real production Storage: a real 20MB file uploaded and registered correctly; a 30MB file was cleanly rejected.
- [x] Fixed a second, unrelated upload failure surfaced retesting the fix above — see CLAUDE.md section 7pp's same-day follow-up. The "What is this document?" field was always required but always started blank with no visual cue, so choosing a file before typing anything into it silently 400'd — pre-existing, just newly reachable. Now pre-fills from BPLO's own open request note when there is one, plus a client-side guard with an accurate message either way. Verified with a real end-to-end upload through the fix.
- [x] Full pipeline audit + the top 3 findings fixed — see CLAUDE.md section 7qq. Traced every role's journey (applicant, BPLO, department, treasury, mayor) end to end; published a 23-item findings report. Fixed the three most serious: OTP verification had no brute-force protection (now locks out after 5 wrong guesses); signing a permit did the irreversible parts — issuing it, publishing a public PDF, texting the applicant — before checking the application was actually ready to sign; and "Reject" at initial review was functionally identical to "Request info," never actually closing anything. Verified each against real production data.
- [x] Working through the rest of the audit — see CLAUDE.md section 7qq's same-day follow-up. Fee overrides now reject garbage/negative input instead of silently corrupting the total; a real double-submit race in finalizing assessments and recording payments is closed (claim the status transition first, so a duplicate can never insert real fee-line or payment rows); Archive now works from any stuck stage, not just "Returned to applicant," and remembers where it was archived from so Reopen restores the right stage; three structural dead-ends in department review closed (a deactivated department no longer silently drops out of the clearing check, a newly-added one no longer blocks an already-open round forever, and zero active departments no longer strands an application with no path forward at all).
- [x] Three more from the audit — "approved with condition" now actually shows the condition to whoever reviews next instead of vanishing into the audit log; a still-open Treasury request now resolves itself once payment is actually recorded, instead of sitting orphaned forever; and the "currently stuck" table on Performance Stats no longer misses "Returned to applicant" or silently hides anything older than the selected date range (it's a live snapshot now, not filtered by the range).
- [x] Document uploads now clean up after themselves — see CLAUDE.md section 7qq's same-day follow-up. A new daily cron sweeps abandoned pre-submission uploads and real Storage files that never got registered at all (12 of the former already existed in production); a new 50-file-per-owner cap stops an unbounded upload burst from ever being possible in the first place.
- [x] Pausing a client now actually stops them — see CLAUDE.md section 7qq's same-day follow-up. Pause previously only blocked a fresh page load and new applicant submissions; a staff member with a tab already open could keep approving, paying, and signing through it indefinitely, since nothing at the action level ever checked. Every state-changing action across BPLO, Treasury, Mayor, department, and Settings now checks for real.
- [x] BPLO could act on a department's behalf using a stale, superseded review round; Treasury and Mayor had no read access to department_reviews at all — see CLAUDE.md section 7qq's same-day follow-up. Both closed, plus the new RLS policy for department_reviews.
- [x] Applicants now have a real sign-out — "Start over" and a new "Not you? Sign out" on the status page actually revoke the session, not just reset the form. See CLAUDE.md section 7qq's same-day follow-up.
- [x] verify-otp now rejects ambiguous request shapes and checks a legacy-business claim's own LGU before allowing it, instead of trusting a different route's scoping. See CLAUDE.md section 7qq's same-day follow-up.
- [x] BPLO can now regenerate a signed permit's PDF/QR from the Business Registry — before this, a failed generation at signing time was permanent with no way to retry. See CLAUDE.md section 7qq's same-day follow-up.
- [x] BPLO can now unlink a business from the wrong owner and let it be reclaimed — closes the last item from the full-pipeline audit (all 23 findings now addressed). See CLAUDE.md section 7qq's same-day follow-up.
- [x] BPLO dashboard's 8 pipeline stat cards are now clickable tabs — only the clicked stage's queue shows below, defaulting to Initial review. See CLAUDE.md section 7rr.
- [x] Performance Stats renamed to Stats & Reports, with a new Reports tab — revenue collected by Barangay Clearance / Engineering / CEDULA / Actual Permit, each downloadable as CSV. See CLAUDE.md section 7ss.
- [x] Settings redesigned — 12 flat, scattered sections regrouped into 6 labeled, bordered categories (Staff & Access, Fee Rates, Barangays, Assessment Rules, Documents & Alerts, Public Application Form). See CLAUDE.md section 7tt.
- [x] Reports tab redesigned into a real 3-level drill-down (category → business subtotal → fee lines) instead of a long flat list repeating each business name per fee line. See CLAUDE.md section 7ss's same-day follow-up.
- [x] Self-service "Import Businesses" in Settings — a new LGU can upload their existing business roster (Excel/CSV) themselves; a row with a phone number is claimed immediately. See CLAUDE.md section 7uu.
- [x] Clarified where LBT categories actually come from — labeling + contextual "Add it in Settings" links wherever a category might be missing. See CLAUDE.md section 7uu's same-day follow-up.
- [x] Import Businesses template gained owner gender, payment frequency, and an informational new/renewal note — plus fixed a real bug where a valid organization_type was silently rejected. See CLAUDE.md section 7uu's same-day follow-up.
- [x] Configurable Permit No. Format per LGU — Settings now has a "Permit No. Format" section (prefix, year width, auto-incrementing counter width, e.g. SMB-2026-000056), with a live preview. The year always stays live-computed; only its display width is a choice. Migration `0051`, live in production. See CLAUDE.md section 7vv.
- [x] Business Tax installment payments — a Renewal can now choose Bi-Annually (half now, reminded Jul) or Quarterly (a quarter now, reminded Apr/Jul/Oct); a New application always pays the full annual amount. Reminder dates are LGU-configurable in Settings, not hardcoded — San Miguel's real dates are seeded, every other LGU starts blank. Reminder-only (no online payment of later installments) per the project owner's own choice. Migration `0052`, live in production. See CLAUDE.md section 7ww.
- [x] Accepted Payment Methods — Settings now lets BPLO turn on Cash, GCash, Bank Transfer, and/or an Online Portal (multiple at once), each with its own details, which now show up in the payment-due SMS/email and on the applicant's status page. GCash/Bank Transfer/Online each point the applicant to the existing document-upload mechanism for proof of payment, and Treasury/BPLO can now see uploaded documents in the Awaiting Payment queue for the first time. Migration `0053`, live in production. See CLAUDE.md section 7xx.
- [x] Self-service department management on `/admin` — departments (Engineering, MHO, MPDO, BFP, MENRO, ...) could previously only ever be set once, at LGU creation. The platform admin can now add, deactivate, and reactivate a client's departments at any time, from `/admin` itself — no code/migration needed anymore. See CLAUDE.md section 7yy.
- [x] SMS usage counter (1000 free/month per municipality, ₱0.55 each over) — shown on `/admin` per client and in each LGU's own Settings, resetting monthly with a history of previous months. Found and fixed a real gap first: OTP sends (likely the majority of real SMS volume) were never logged or attributed to an LGU at all. Migration `0054`, live in production. See CLAUDE.md section 7zz.
- [x] Nature of Business is now a real searchable dropdown instead of a plain `<select>` — typing filters the ~220 options by substring instead of only jumping to the first option starting with the last key pressed. Caught and fixed a real reopen bug during verification. See CLAUDE.md section 7a1.
- [x] Vercel function region moved from Virginia (`iad1`) to Tokyo (`hnd1`), next to the Supabase database — closes out an item from the pipeline audit about slow tab-switching. See CLAUDE.md section 7a2. A Singapore database migration is a possible next step if this alone isn't enough; deferred pending observation.
- [x] Full-system QA sweep (13 scenarios, fully synthetic disposable test LGU, zero real notifications sent) found and fixed 4 real bugs: renewal applications with Bi-Annually business tax could never be finalized (a live production bug from the same-day installment-payments feature — migration `0055`), reopening a rejected application restored the wrong stage, the Reports page mislabeled the essential-commodity discount, and two department-review checks failed silently on a real query error instead of surfacing it. See CLAUDE.md section 7a3.
- [x] Full-system audit (UI/design, mobile, accessibility, code quality, security, backup/DR, error monitoring, data privacy, performance — 45 findings) found and fixed: a critical cross-tenant RLS gap on `department_reviews` (migration `0056`), OTP SMS endpoints rate-limited by IP (migration `0057`), a text-overlap bug across all 4 PDF generators, a lint gate finally running before every build, and a large batch of mobile/accessibility/design fixes on both the applicant form and the staff dashboard. Non-code items (Supabase backups, Sentry, Vercel Analytics, the DPA/RA 10173 legal review) are still open — see CLAUDE.md sections 7a4–7a11.
- [x] Settings status pills — every one of the 15 collapsed Settings sections now shows a live status at a glance (e.g. whether Automated Assessment is on, which payment methods are enabled), from a real Artifact mockup approved before it was built. See CLAUDE.md section 7a11.
- [x] Notes are now genuinely mandatory (not just a hint) for Approve-with-condition/Request-info/Reject, on all three surfaces (BPLO initial review, department review including BPLO-on-behalf, Treasury) — the audit's own fix only made the "required" text persist visually, it never actually enforced anything. Also closed a related gap: BPLO acting on a department's behalf had no notes field at all before this. See CLAUDE.md section 7a12.
- [x] Additional-document uploads (info-request responses, payment proof) now have a real "Upload successful → Send" step instead of registering and notifying staff the instant the file finishes uploading — matches how people actually expect an upload-then-send flow to work. Covers every applicant-facing upload of this kind; the main application form's own document fields already worked this way (one file at a time, one "Submit application" at the end). See CLAUDE.md section 7a13.
- [x] A document sent in response to a department/BPLO/Treasury request now shows staff exactly what it's answering — a highlighted card ("↩ Sent in response to MHO's request: ...") instead of blending into the same flat list as the original application's documents. Confirmed staff notifications on send were already working correctly. Migration `0058`, live in production. See CLAUDE.md section 7a14.
- [x] Every applicant-facing SMS/email rebuilt: plain, short language (most real applicants aren't confident English readers) and one shared branded email template (LGU seal, "Hi `<FirstName>`,", one obvious button) instead of bare paragraphs — plus a new "LGU Logo" upload in Settings, the missing "upload your payment proof" link on the assessment-finalized email, and (same-day follow-up) the logo now also prints on the Order of Payment PDF. See CLAUDE.md section 7a15.
- [x] New Fire Safety Inspection Fee (FSIF) notice, sent right after BPLO's initial approval whenever BFP is one of the LGU's active departments — explains in plain language that BFP works independently from the LGU, must be paid separately at their own national e-BFP portal, and links to the applicant's status page to upload proof (the same mechanism BFP's own review queue reads from). BFP's own contact info is looked up live from its staff account(s) — no separate Settings field to keep in sync. See CLAUDE.md section 7a16.
- [x] Fixed Performance Stats' "Current bottleneck" banner — it always headlined the slowest of the 5 reviewing departments even when Payment or Printing took far longer overall, since it never actually compared against the other pipeline stages. Now picks the genuinely slowest stage, drilling into the slowest department only when Departments review itself is that stage. See CLAUDE.md section 7a17.
- [x] Applicant form (`/apply`) widened on desktop (640px → 780px) and every font size bumped up one step on the same scale, including the shared input/select style — more comfortable to read, especially for older applicants. Confirmed the mobile responsiveness fixed in an earlier session is untouched (no media query involved either way). See CLAUDE.md section 7a18.
- [x] Fixed the LBT category dropdown visually reverting to "Not set" right after a real save (on both BPLO's Initial Review card and the Business Registry's own editor) — the save was always correct, the same React 19 "form resets after action" bug already fixed twice elsewhere in this project, just on an uncontrolled `<select>` this time. See CLAUDE.md section 7a19.
- [x] A GCash/Bank/Online or FSIF payment-proof upload now gets its own highlighted flag on staff's document list too, not just the existing request-more-info responses — new `documents.purpose` column, validated server-side against a known set so a tampered value can never fake the highlight. See CLAUDE.md section 7a20.
- [x] Dashboard top bar's avatar is now a real profile menu (staff member's own name + Sign out), and a new "Knowledge Base" link took over the spot the plain Sign out pill used to occupy — opens `/dashboard/knowledge-base` in a new tab (content is the next task; this is a real placeholder page, not a dead link). See CLAUDE.md section 7a21.
- [x] Wrote the real Knowledge Base content — Getting Started, the Review Pipeline, Businesses, all 7 Settings groups, Audit Trail & Stats, a full notifications-reference table, and FAQ. Every "screenshot" is a real recreation using this app's own design tokens (not a raster image), with a `blur-sm` treatment over any example municipality name. See CLAUDE.md section 7a22.
- [x] Walk-in filings can now attach a scan or phone photo of the requirements — a walk-in renewal/new-permit skips initial review entirely and opens department review right away, so without this, Engineering/MHO/MPDO/BFP/MENRO were reviewing with zero documents attached. One "Scan / upload file" button on the walk-in form, direct-to-Storage (same technique as every other upload in this app), attached to the application the moment it's created. Same-day follow-up: BPLO's own "In department review" card never showed an application's documents at all — fixed so BPLO sees exactly what a department sees. See CLAUDE.md section 7a23.
- [x] The "Sworn Statement of Gross Sales" upload for renewals is no longer mandatory — checked the real source form first (it's actually marked required there too, so this is a deliberate project-owner override, not a rubber-stamp), still shown, just doesn't block submission if left blank. Same-day follow-up, the mirror image: the Barangay Clearance upload is now mandatory when the applicant answers "Yes" to having one (source form marks it optional too — another deliberate override). See CLAUDE.md section 7a24.
- [x] Removed every "MuniServe" mention visible to an applicant or staff member — page titles/headings, PDF footers ("Generated by MuniServe" and similar), applicant SMS/email copy, staff-facing Settings/Knowledge Base text, and CSV export filenames. Left untouched: code comments (never rendered), the actual `muniserve.ph` domain (real infrastructure), and `/admin` (the platform owner's own console, not client-facing). See CLAUDE.md section 7a25.
- [x] The applicant form's "Resend code" button now actually disables with a live 30-second countdown instead of always being clickable — matches the real per-phone cooldown the server already enforced. Found and fixed the identical gap on the status page's own phone re-verification card too. See CLAUDE.md section 7a26.
- [x] Staff/department email notifications now use the same branded template as applicant emails (LGU seal, personalized greeting, one CTA button) instead of a bare paragraph — and every one of them now names the business and owner, not just a reference number. Covers 13 call sites: new applications, department round openings, all-cleared/ready-for-assessment, payment/print/release, rejections, resubmissions, uploads, and the 24-hour reminder. See CLAUDE.md section 7a27.
- [x] The applicant form now prefills Province, City/Town, and Zip Code — the first two already existed per-LGU, only Zip Code was new (a Settings field, "Address Defaults"). Still editable, not locked, so a rare business whose actual detail genuinely differs isn't blocked. See CLAUDE.md section 7a28.
- [x] Delivery Service — an applicant whose permit is ready for release can request delivery instead of picking it up, from a button on their own status page. New Settings toggle (courier name + phone, off by default); the courier gets a text with the owner's name, phone, and address; BPLO's release queue flags which applications chose delivery. See CLAUDE.md section 7a29.
- [ ] Whatever's next — see CLAUDE.md section 10 for what's still open (mostly blocked on legal/LGU confirmation, not code)

## What needs to happen before development continues

1. **Create the Supabase project** (supabase.com), then run the two migrations in `supabase/migrations/` in order via the SQL editor or `supabase db push`.
2. **Create a GitHub repo** and push this folder to it.
3. **Create a Vercel project** connected to that repo for auto-deploy on push.
4. **Env vars** — done for Supabase + Resend, both locally in `.env.local` and in Vercel. Still missing: `SEMAPHORE_API_KEY` (needed for step 5, applicant OTP flow — not urgent yet).

None of the above requires a code change — this is account creation and configuration, not something to script.
